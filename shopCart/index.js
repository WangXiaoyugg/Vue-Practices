// 初始化vue 实例const app = new Vue({   el:'#app',   data:{       list:[           {               id:1,               name:'iphone X',               price:8388,               count:1           },           {               id:2,               name:'iPad Pro',               price:5888,               count:1           },           {               id:3,               name:'MacBook Pro',               price:13288,               count:1           },       ]   },   computed:{       totalPrice:function () {           let total = this.list.reduce((acc,cur) => {              return acc+cur.price*cur.count           },0);           //实现带有千分位的分隔符           // https://www.zhihu.com/question/29999924           return total.toString().replace(/\B(?=(\d{3})+$)/g,',')       }   },   methods:{        handleReduce:function (index) {            if(this.list.count === 1) return;            this.list[index].count--;        },        handleAdd:function (index) {            this.list[index].count++        },        handleRemove:function (index) {            this.list.splice(index,1)        }   }});